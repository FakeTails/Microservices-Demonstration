doctype html
head
  meta(charset='utf-8')
  meta(name='viewport' content='width=device-width, initial-scale=1')
  // Latest compiled and minified CSS
  link(rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css')
  // jQuery library
  script(src='https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js')
  // Latest compiled JavaScript
  script(src='https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js')
  link(rel='stylesheet' type='text/css' href='/css/style.css')
header
  // NAVBAR
  nav.navbar.navbar-inverse
    .container-fluid
      .navbar-header
        button.navbar-toggle(type='button' data-toggle='collapse' data-target='#myNavbar')
          span.icon-bar
          span.icon-bar
          span.icon-bar
        a.navbar-brand(href='/') raveRSVP
      #myNavbar.collapse.navbar-collapse
        ul.nav.navbar-nav
          li
            a(href='/') Home
          li.active
            a(href='/rsvp') RSVP
          //
            <li><a href="#">Page 2</a></li>
            <li><a href="#">Page 3</a></li>
        ul.nav.navbar-nav.navbar-right
          li
            a(href='/logout')
              span.glyphicon.glyphicon-user
              |  Logout
  // NAVBAR EOF
.container-fluid
  .row
    .col-sm-12.titleBox
      h1 Upcoming Shows
      p
        | Make sure the party is aware of your impending arrival!

  .row
    .col-sm-4
    .col-sm-4.titleBox
      .border
        p
          | Enter the name of the venue:
          form(action='/rsvp')
            input(type="text" name="search") 
            if (noResults)
              |  No venues found!
            if (results)
              |  Venues found!
            .row.boxRow
              .col-xs-12
                button.button1(type='submit') Search
    .col-sm-4

  .row
    .col-sm-12.titleBox   
      p
        | OR
      p
        | Select a venue below:

      form(action='/checkout')
        #venues.row.boxRow
          each venue in venues 
            .col-sm-4.venuebox
              img(src='/images/venueMap.png' alt='Places to rave' width='60%' height='60%')
              p.venueName
                input(id=`${venue.name}` name="venue" type="radio" value=`${venue.name}`) 
                |  #{venue.name}
              
        .row
          .col-xs-12.boxRow2
            button.button2(type='submit') RSVP